"""Alembic migration package.

Migrations are an **explicit deployment operation**. They are never executed
automatically during application startup, and never contain destructive DDL
(``DROP DATABASE``, ``TRUNCATE``, etc.). Apply with ``alembic upgrade head``.
"""
