pydantic>=2.7.0
pydantic-settings>=2.2.0
httpx>=0.27.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
fastapi>=0.111.0
uvicorn>=0.30.0
numpy>=1.26.0

# --- B2: PostgreSQL + PostGIS Foundation ---
# SQLAlchemy 2.x async (the [asyncio] extra pulls greenlet) + asyncpg driver.
SQLAlchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
# PostGIS-compatible spatial column types.
GeoAlchemy2>=0.15.0
# Migrations (async-capable, environment-driven URL).
alembic>=1.13.0
